import React, {useState} from "react";
import "./product-select.css";

const ProductSelect = ({ products, setProduct }) => {
    // We're going to show products first
    

    
}

export default ProductSelect;